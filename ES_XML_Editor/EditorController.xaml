<Window x:Class="ES_XML_Editor.EditorController"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EditorController" Height="300" Width="300" Opacity="0" AllowsTransparency="True" WindowStyle="None" ShowActivated="False" Activated="programActivated">
    <Grid>
        <!--
        <Grid.Resources>
            <DataTemplate x:Key="fileNameTemplate">
                <TextBlock Text="{Binding Path=name}"/>
            </DataTemplate>
            
            <DataTemplate x:Key="fileTypeTemplate">
                <TextBlock Text="{Binding Path=kind}"/>
            </DataTemplate>
        </Grid.Resources>
        <TabControl >
            <TabItem Header="folder browser">
                <ListView x:Name="testListView" SelectionMode="Single" MouseDoubleClick="listViewItemDoubleCLick">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" CellTemplate="{StaticResource fileNameTemplate}"/>
                            <GridViewColumn Header="Type" CellTemplate="{StaticResource fileTypeTemplate}"/>
                        </GridView>
                    </ListView.View>

                </ListView>
            </TabItem>
        </TabControl>
        -->
    </Grid>
</Window>
